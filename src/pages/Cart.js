import { BarClient } from "../components/BarClient"
import { CartProducts } from "../components/CartProducts"

export const Cart = ()=>{
    return(
        <>
        <BarClient/>
        <CartProducts/>
        </>
    )
}