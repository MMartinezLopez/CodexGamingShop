import { AppBar } from "@mui/material"
import { ListProducts } from "../components/ListProducts"

export const Client = () => {
    return (
        <>
        <AppBar/>
        <ListProducts/>
        </>
    )
}